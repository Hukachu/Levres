@model IEnumerable<Levres.Models.Konfiguracija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.boja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.felge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vrsta_interijera)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.svjetla)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.motor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.boja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.felge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vrsta_interijera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.svjetla)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.motor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
